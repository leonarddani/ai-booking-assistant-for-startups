version: "3.9"

services:
  booking-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-booking-assistant
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./app:/app
      - ./credentials.json:/app/credentials.json:ro
